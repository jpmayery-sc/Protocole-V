# HF Transport Data

This folder holds the protocol V2 transport datasets.

Required dataset shape:
- material symbol
- frequency
- observed transport observable
- uncertainty, if available
- metadata for the published source

These files are normalized real datasets with explicit provenance. Keep the manifest in sync with the published sources and derived values.
